# 🧪 SQL Judge Exam — 测试汇总报告

**测试日期**: 2026-06-12  
**最近复核/修复**: 2026-06-13 00:xx  
**测试范围**: 后端 API (18 个端点) + 前端 12 个页面 + 3 种角色鉴权  
**服务状态**: ✅ 后端 8080 / ✅ 前端 5173 均正常运行

> 复核说明：本报告中的问题已逐项验证和修复。当前剩余说明项均为环境提示或兼容性说明，不再包含阻塞性功能缺口。

---

## 一、功能完成度总览

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户鉴权 (JWT) | ✅ 完成 | 登录、登出、token 验证、密码修改均正常 |
| 题目浏览 | ✅ 完成 | 分页列表 + 关键词搜索 + 详情页 |
| SQL 在线判题 | ✅ 完成 | 临时 schema 隔离执行，测试用例比对 |
| 提交记录 | ✅ 完成 | 提交保存 + 历史列表 |
| 考试管理 | ✅ 基础完成 | 创建/编辑/归档/发布/添加题目/添加学生可用 |
| 成绩统计 | ✅ 基础完成 | 后端分数计算正常；前端成绩页已接入考试选择和成绩 API |
| 班级管理 | ✅ 基础完成 | 列表正常；学生可通过邀请码加入班级 |
| 仪表盘 | ✅ 基础完成 | 学生/教师核心指标已从数据库计算 |
| 管理员用户管理 | ✅ 基础完成 | 管理员可查看用户、创建用户、调整角色和状态 |
| 用户注册 | 🔒 已锁定 | 设计如此——第一版不开放公开注册 |

---

## 二、API 端点测试结果 (18/18)

| 端点 | 方法 | 测试结果 |
|------|------|----------|
| `/api/auth/login` | POST | ✅ 三种角色均可登录，错误密码/空用户名均正确拒绝 |
| `/api/auth/me` | GET | ✅ 返回当前用户信息 |
| `/api/auth/register` | POST | ✅ 正确拒绝 (40300: 不开放注册) |
| `/api/questions` | GET | ✅ 支持分页 `page/size`；关键词同时匹配标题和描述 |
| `/api/auth/change-password` | POST | ✅ 已登录用户可修改密码；旧密码错误/新密码过短会拒绝 |
| `/api/questions/{id}` | GET | ✅ 学生详情脱敏；教师/管理员详情返回 answerSql/testCases |
| `/api/judge/run` | POST | ✅ 判题正确；正确拦截非 SELECT/空 SQL/多语句 |
| `/api/submissions` | POST | ✅ 提交保存；关联考试自动计分 |
| `/api/submissions/mine` | GET | ✅ 返回提交历史 |
| `/api/exams` | GET/POST | ✅ 教师创建/学生查看均正常 |
| `/api/exams/{id}` | GET | ✅ 考试详情正常 |
| `/api/exams/{id}/questions` | GET/POST | ✅ 已脱敏异常；添加不存在题目 ID 不再泄漏 SQL 细节 |
| `/api/exams/{id}/students` | POST | ✅ 添加学生成功 |
| `/api/exams/{id}/publish` | POST | ✅ 发布成功 |
| `/api/exams/{id}/scores` | GET | ✅ 分数列表正常 |
| `/api/classes` | GET | ✅ 教师/学生根据角色返回对应班级 |
| `/api/classes/join` | POST | ✅ 学生可通过邀请码加入班级；教师调用被拒绝 |
| `/api/student/dashboard` | GET | ✅ 数据正常 |
| `/api/teacher/dashboard` | GET | ✅ `activeClasses/pendingReviews/recentExams` 从数据库查询 |
| `/api/admin/users` | GET/POST/PUT | ✅ 管理员可管理用户；教师访问被拒绝 |

---

## 三、前端页面测试结果 (12/12 可访问)

| 页面 | 路由 | 状态 |
|------|------|------|
| 登录页 | `/login` | ✅ 完整可用 |
| 题库列表 | `/problems` | ✅ 服务端分页搜索 |
| 题目详情+编辑器 | `/problems/:id` | ✅ 运行/提交均正常 |
| 学生仪表盘 | `/student/dashboard` | ✅ 数据显示正常 |
| 学生考试列表 | `/student/exams` | ✅ |
| 考试作答 | `/student/exams/:id/take` | ✅ 已支持题号导航、上一题/下一题切换，并为每题保留独立草稿和运行结果 |
| 学生提交记录 | `/student/submissions` | ✅ |
| 学生班级 | `/student/classes` | ✅ |
| 教师仪表盘 | `/teacher/dashboard` | ✅ |
| 教师题库管理 | `/teacher/questions` | ✅ 已支持题目新增、编辑、软删除，并可查看答案/测试用例 |
| 教师班级管理 | `/teacher/classes` | ✅ |
| 教师成绩统计 | `/teacher/scores` | ✅ 已接入考试列表和 `/api/exams/{id}/scores` 成绩表 |
| 考试配置向导 | `/teacher/exams/new` | ✅ 三步流程可用：配置考试、选择题目、设置分值并保存 |
| 管理员用户管理 | `/admin/users` | ✅ 用户列表、新增用户、角色/状态调整可用 |

---

## 四、发现的问题汇总

### 🔴 安全/严重问题 (2 项，已修复)

**1. SQL 异常信息泄漏 — ✅ 已修复**  
[GlobalExceptionHandler.java](backend/src/main/java/com/sqljudge/exam/common/GlobalExceptionHandler.java) 已改为服务端记录日志、客户端统一返回 `服务端错误，请联系管理员`。复核命令触发添加不存在题目 ID 后，响应为 `{"code":50000,"message":"服务端错误，请联系管理员","data":null}`，不再包含 SQL/表/约束名。

**2. API 层缺少角色鉴权 — ✅ 已修复核心接口**  
已给教师/学生仪表盘、考试创建/发布/加题/加学生/成绩、班级加入接口补充 `@PreAuthorize`。复核结果：`student1` 访问 `/api/teacher/dashboard` 和 `POST /api/exams` 均返回 `40300 权限不足`，`teacher1` 可访问 `/api/teacher/dashboard`。

**3. 题目答案字段泄漏 — ✅ 已修复**  
复核仪表盘时发现 `/api/questions` 和学生仪表盘推荐题曾返回 `answerSql`。现已将题目列表与推荐题改为 `QuestionSummary` 脱敏 DTO；复核结果：学生题目列表和学生仪表盘响应不再包含 `answerSql`，教师详情仍可查看 `answerSql/testCases`。

### 🟡 功能桩/未完成 (7 项)

| # | 位置 | 描述 |
|---|------|------|
| 3 | [ClassService.java](backend/src/main/java/com/sqljudge/exam/modules/classes/ClassService.java) | ✅ 已修复：学生可通过邀请码加入班级，非学生调用被拒绝 |
| 4 | [ExamWizard.vue](frontend/src/views/teacher/ExamWizard.vue) | ✅ 已修复：`save()` 已接入 `POST /api/exams`，包含基础校验、提交中状态、成功/失败提示；复核创建考试返回 `code=0` 和 DRAFT 考试 ID |
| 5 | [TeacherScores.vue](frontend/src/views/teacher/TeacherScores.vue) | ✅ 已修复：页面加载 `/api/exams`，支持选择考试并调用 `/api/exams/{id}/scores` 展示学生成绩、状态、平均分和最高分 |
| 6 | [QuestionDetailResponse.java](backend/src/main/java/com/sqljudge/exam/modules/question/QuestionDetailResponse.java) | ✅ 已修复：DTO 增加 `answerSql/testCases`，教师/管理员详情可见；学生详情仍脱敏 |
| 7 | [TeacherQuestions.vue](frontend/src/views/teacher/TeacherQuestions.vue) | ✅ 已修复：题库管理支持新增、编辑、软删除题目，教师可查看答案和测试用例 |
| 8 | [ExamTake.vue](frontend/src/views/student/ExamTake.vue) | ✅ 已修复：考试题目列表完整保存为数组，支持题号导航和上一题/下一题切换；运行/提交使用当前题 `questionId` |
| 9 | [ExamController.java](backend/src/main/java/com/sqljudge/exam/modules/exam/ExamController.java) | ✅ 已修复：新增 `PUT /api/exams/{examId}` 编辑考试，`DELETE /api/exams/{examId}` 将考试归档为 `ARCHIVED` |

### 🟡 设计/体验问题 (8 项)

| # | 描述 |
|---|------|
| 10 | ✅ 已修复：`streakDays` 按学生 AC 提交日期计算，`activeClasses` 按教师班级数查询，`pendingReviews` 按待处理提交查询 |
| 11 | ✅ 已修复：题目关键词搜索已同时匹配 `title` 和 `description`；复核 `keyword=无论 person` 可命中 175 题 |
| 12 | ✅ 已修复：`/api/questions?page=&size=` 返回 `items/total/page/size`，学生题库页和教师题库页已使用服务端分页 |
| 13 | ✅ 已修复：AppLayout 文档/帮助链接改为有效路由，通知按钮显示状态提示，设置按钮打开修改密码面板 |
| 14 | ✅ 已修复：新增 `POST /api/auth/change-password`，前端设置面板支持修改密码；复核已将 `student1` 临时改密后恢复默认密码 |
| 15 | ✅ 已修复：新增 `/api/admin/users` 管理员 API 和 `/admin/users` 页面；复核管理员可创建/禁用用户，教师调用返回 `40300 权限不足` |
| 16 | ✅ 已修复：ExamWizard 三步流程已可用，支持分页选题、题目增删、设置顺序和分值，并调用 `/api/exams/{id}/questions` 保存 |
| 17 | ✅ 已修复：`/api/submissions/mine` SQL 显式返回 camelCase 字段，前端提交记录页不再兼容读取 snake_case 字段 |

### 🔵 技术债务 (3 项)

| # | 描述 |
|---|------|
| 18 | ✅ 已处理：Flyway 已从 8.5.13 升级到 Java 11 兼容的 9.22.3；本机 MySQL 9.6 高于 Redgate 当前文档列出的 MySQL 9.4 支持范围，剩余日志为环境兼容提示，迁移和启动已验证通过 |
| 19 | ✅ 已修复：移除未使用的 `echarts`、`monaco-editor` 依赖和 Vite optimizeDeps 配置，`package-lock.json` 已同步 |
| 20 | ✅ 已修复：异常响应返回真实 HTTP 状态码并保留业务错误体；已复核 400/401/403/404/500 场景 |

---

## 五、角色鉴权测试

| 测试场景 | admin | teacher1 | student1 | 未登录 |
|----------|-------|----------|----------|--------|
| 登录 | ✅ | ✅ | ✅ | — |
| 查看题目列表 | ✅ | ✅ | ✅ | 未登录返回 HTTP 401 |
| 运行判题 | ✅ | ✅ | ✅ | 未登录返回 HTTP 401 |
| 提交答案 | ✅ | ✅ | ✅ | 未登录返回 HTTP 401 |
| 查看考试列表 | ✅ | ✅ (创建的) | ✅ (分配的) | 未登录返回 HTTP 401 |
| 创建考试 | ✅ | ✅ | 学生返回 HTTP 403 | 未登录返回 HTTP 401 |
| 查看班级 | ✅ | ✅ | ✅ | 未登录返回 HTTP 401 |
| 教师仪表盘 API | ✅ | ✅ | 学生返回 HTTP 403 | 未登录返回 HTTP 401 |
| 学生仪表盘 API | ✅ | ✅ | ✅ | 未登录返回 HTTP 401 |
| 前端教师路由 | ✅ | ✅ | 前端守卫重定向到学生仪表盘 | 跳转登录页 |

> 本轮复核已确认核心教师 API 增加了后端 `@PreAuthorize` 保护。

---

## 六、判题系统测试

| 测试场景 | 输入 | 结果 |
|----------|------|------|
| 正确 SELECT (但结果不匹配) | `SELECT * FROM employee` | ⚖️ WA (Wrong Answer)，得分 0 |
| 非 SELECT 语句 | `DROP TABLE employee` | 🚫 FORBIDDEN: 只允许查询类SQL |
| 空 SQL | `""` | HTTP 400 / 40000: SQL不能为空 |
| 多语句 | `SELECT 1; SELECT 2` | HTTP 403 / 40300: 不允许多语句提交 |
| 不存在的题目 ID | questionId=9999 | HTTP 404 / 40400: 题目不存在 |
| 关联考试提交 | questionId=1, examId=3 | ✅ 提交成功，自动触发考试计分 |

判题流程：用户 SQL → JSqlParser 语法校验 → 只允许 SELECT → 创建临时 schema → 执行测试用例的 input_sql 建表 → 执行用户 SQL → 读取结果集 → 与 expected_output JSON 比对 → 清理临时 schema → 返回结果

---

## 七、数据库状态

- **数据库**: MySQL 9.6 / `sql_exam`
- **Flyway 迁移**: 4 个脚本全部执行成功 (V1~V4)
- **用户数**: 3 个默认账号 (admin, teacher1, student1)
- **题目数**: 11 道 (1 道示例题 + 10 道 LeetCode 175-185)
- **班级数**: 1 个 ("数据库课程一班", 邀请码 `DB2026A`)
- **考试数**: 4 个 (含本轮 `Codex Save Verification` 接口验证创建的 DRAFT 考试)

---

## 八、总结评价

**整体完成度**: 约 **95%** — 核心判题、考试、题库、成绩、班级、教师常用流程、基础管理员用户管理和 HTTP 错误状态码均已可用。

**可用场景**: 
- ✅ 学生可登录、浏览题目、在线编写 SQL 并判题、参加考试
- ✅ 教师可创建考试、添加题目和考生、在成绩页查看分数
- ✅ 教师可管理题库，并查看基础成绩统计
- ✅ 管理员可进行基础用户管理

**优先改进建议**:

| 优先级 | 改进项 |
|--------|--------|
| **P0** | ✅ 已完成：修复异常信息泄漏（`GlobalExceptionHandler` 脱敏）+ 添加核心 API 层 `@PreAuthorize` 角色鉴权 |
| **P1** | ✅ 已完成：补全班级加入、`ExamWizard.save()`、TeacherScores 页面、题目基础 CRUD |
| **P2** | ✅ 已完成：考试作答多题切换导航、仪表盘动态指标、ExamWizard 选题/配分 |
| **P3** | ✅ 已完成：服务端分页、死链接修复、密码修改功能、清理冗余依赖、HTTP 状态码规范化 |
