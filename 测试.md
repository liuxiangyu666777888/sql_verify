# 🧪 SQL Judge Exam — 测试汇总报告

**测试日期**: 2026-06-12  
**最近复核/修复**: 2026-06-12 23:xx  
**测试范围**: 后端 API (18 个端点) + 前端 12 个页面 + 3 种角色鉴权  
**服务状态**: ✅ 后端 8080 / ✅ 前端 5173 均正常运行

> 复核说明：本报告中的问题已开始逐项验证和修复。已验证并修复 P0 安全问题，以及 `/api/classes/join` 空桩。未标记为“已修复”的条目仍需继续完善。

---

## 一、功能完成度总览

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户鉴权 (JWT) | ✅ 完成 | 登录、登出、token 验证均正常 |
| 题目浏览 | ✅ 完成 | 列表 + 关键词搜索 + 详情页 |
| SQL 在线判题 | ✅ 完成 | 临时 schema 隔离执行，测试用例比对 |
| 提交记录 | ✅ 完成 | 提交保存 + 历史列表 |
| 考试管理 | ⚠️ 部分完成 | 创建/发布/添加题目/添加学生可用；无编辑/删除 |
| 成绩统计 | ⚠️ 部分完成 | 后端分数计算正常；前端成绩页为空白桩 |
| 班级管理 | ✅ 基础完成 | 列表正常；学生可通过邀请码加入班级 |
| 仪表盘 | ⚠️ 部分完成 | 基础数据显示；多项指标硬编码 |
| 用户注册 | 🔒 已锁定 | 设计如此——第一版不开放公开注册 |

---

## 二、API 端点测试结果 (18/18)

| 端点 | 方法 | 测试结果 |
|------|------|----------|
| `/api/auth/login` | POST | ✅ 三种角色均可登录，错误密码/空用户名均正确拒绝 |
| `/api/auth/me` | GET | ✅ 返回当前用户信息 |
| `/api/auth/register` | POST | ✅ 正确拒绝 (40300: 不开放注册) |
| `/api/questions` | GET | ✅ 返回 11 道题目；关键词搜索仅匹配标题 |
| `/api/questions/{id}` | GET | ✅ 详情正常；⚠️ 未返回 testCases/answerSql |
| `/api/judge/run` | POST | ✅ 判题正确；正确拦截非 SELECT/空 SQL/多语句 |
| `/api/submissions` | POST | ✅ 提交保存；关联考试自动计分 |
| `/api/submissions/mine` | GET | ✅ 返回提交历史 |
| `/api/exams` | GET/POST | ✅ 教师创建/学生查看均正常 |
| `/api/exams/{id}` | GET | ✅ 考试详情正常 |
| `/api/exams/{id}/questions` | GET/POST | ✅ 已脱敏异常；添加不存在题目 ID 不再泄漏 SQL 细节 |
| `/api/exams/{id}/students` | POST | ✅ 添加学生成功 |
| `/api/exams/{id}/publish` | POST | ✅ 发布成功 |
| `/api/exams/{id}/scores` | GET | ✅ 分数列表正常 |
| `/api/classes` | GET | ✅ 教师/学生根据角色返回对应班级 |
| `/api/classes/join` | POST | ✅ 学生可通过邀请码加入班级；教师调用被拒绝 |
| `/api/student/dashboard` | GET | ✅ 数据正常 |
| `/api/teacher/dashboard` | GET | ⚠️ 硬编码值 (activeClasses=1, pendingReviews=0) |

---

## 三、前端页面测试结果 (12/12 可访问)

| 页面 | 路由 | 状态 |
|------|------|------|
| 登录页 | `/login` | ✅ 完整可用 |
| 题库列表 | `/problems` | ✅ 客户端过滤搜索 |
| 题目详情+编辑器 | `/problems/:id` | ✅ 运行/提交均正常 |
| 学生仪表盘 | `/student/dashboard` | ✅ 数据显示正常 |
| 学生考试列表 | `/student/exams` | ✅ |
| 考试作答 | `/student/exams/:id/take` | ⚠️ 仅显示第一道题，无题目切换 |
| 学生提交记录 | `/student/submissions` | ✅ |
| 学生班级 | `/student/classes` | ✅ |
| 教师仪表盘 | `/teacher/dashboard` | ✅ |
| 教师题库管理 | `/teacher/questions` | ⚠️ 仅可查看，无 CRUD |
| 教师班级管理 | `/teacher/classes` | ✅ |
| 教师成绩统计 | `/teacher/scores` | ❌ 空白桩——硬编码"暂无成绩统计数据" |
| 考试配置向导 | `/teacher/exams/new` | ❌ 仅步骤 1/3 有表单，`save()` 为空桩 |

---

## 四、发现的问题汇总

### 🔴 安全/严重问题 (2 项，已修复)

**1. SQL 异常信息泄漏 — ✅ 已修复**  
[GlobalExceptionHandler.java](backend/src/main/java/com/sqljudge/exam/common/GlobalExceptionHandler.java) 已改为服务端记录日志、客户端统一返回 `服务端错误，请联系管理员`。复核命令触发添加不存在题目 ID 后，响应为 `{"code":50000,"message":"服务端错误，请联系管理员","data":null}`，不再包含 SQL/表/约束名。

**2. API 层缺少角色鉴权 — ✅ 已修复核心接口**  
已给教师/学生仪表盘、考试创建/发布/加题/加学生/成绩、班级加入接口补充 `@PreAuthorize`。复核结果：`student1` 访问 `/api/teacher/dashboard` 和 `POST /api/exams` 均返回 `40300 权限不足`，`teacher1` 可访问 `/api/teacher/dashboard`。

### 🟡 功能桩/未完成 (7 项)

| # | 位置 | 描述 |
|---|------|------|
| 3 | [ClassService.java](backend/src/main/java/com/sqljudge/exam/modules/classes/ClassService.java) | ✅ 已修复：学生可通过邀请码加入班级，非学生调用被拒绝 |
| 4 | [ExamWizard.vue:106-108](frontend/src/views/teacher/ExamWizard.vue#L106-L108) | `save()` 函数仅有注释占位 "The backend API can be wired here"，点击保存无任何效果 |
| 5 | [TeacherScores.vue](frontend/src/views/teacher/TeacherScores.vue) | 整个页面为空白桩，硬编码 "暂无成绩统计数据"，未调用任何 API |
| 6 | [QuestionDetailResponse.java](backend/src/main/java/com/sqljudge/exam/modules/question/QuestionDetailResponse.java) | 题目详情 DTO 不包含 `testCases` 和 `answerSql` 字段，教师也无法查看答案和测试用例 |
| 7 | [TeacherQuestions.vue](frontend/src/views/teacher/TeacherQuestions.vue) | 题库管理只有列表查看，无创建题目/编辑题目/删除题目功能 |
| 8 | [ExamTake.vue:105](frontend/src/views/student/ExamTake.vue#L105) | 考试作答页只取 `data[0]` 第一道题，考试含多题时无法切换 |
| 9 | [ExamController.java](backend/src/main/java/com/sqljudge/exam/modules/exam/ExamController.java) | 考试模块缺少编辑(PUT)和删除(DELETE)端点 |

### 🟡 设计/体验问题 (8 项)

| # | 描述 |
|---|------|
| 10 | 仪表盘数据硬编码 — `streakDays` 始终为 0、`activeClasses` 硬编码为 1、`pendingReviews` 硬编码为 0，后端未从数据库计算 |
| 11 | 关键词搜索仅匹配标题 — [QuestionMapper.java:13](backend/src/main/java/com/sqljudge/exam/modules/question/QuestionMapper.java#L13) 的 LIKE 查询只匹配 `title` 字段，不搜索 `description`。前端 [QuestionList.vue:46](frontend/src/views/QuestionList.vue#L46) 额外做了客户端过滤但数据已全量加载 |
| 12 | 题目列表无分页 — 当前 11 题可接受，但数据库增长后无服务端分页会成为性能瓶颈 |
| 13 | AppLayout 中多个链接为死链接 — 文档(`#`)、帮助中心(`#`)、通知按钮、设置按钮均无实际功能 |
| 14 | 无密码修改功能 — 前端和后端均未提供修改密码的入口 |
| 15 | 无管理员用户管理界面 — 管理员角色存在但无对应的用户管理页面或 API |
| 16 | ExamWizard 显示 3 步骤标签 (Configuration / Select Problems / Assign Points)，但仅实现了步骤 1 |
| 17 | Submission 列表字段名不一致 — 后端 MyBatis 返回 Map 使用 snake_case (`submission_id`, `submit_time`)，前端兼容处理 (`item.submission_id \|\| item.submissionId`) |

### 🔵 技术债务 (3 项)

| # | 描述 |
|---|------|
| 18 | Flyway 版本不兼容 MySQL 9.6 — 启动日志警告: "MySQL 9.6 is newer than this version of Flyway and support has not been tested"，建议升级 Flyway |
| 19 | 冗余依赖 — `vite.config.ts` 中配置了 `echarts` 和 `monaco-editor` 的优化排除，但项目中未实际使用这两个库 |
| 20 | HTTP 状态码不规范 — 所有异常（包括 404/403/500）统一返回 HTTP 200 + 业务错误码，不符合 REST 惯例，可能影响监控和日志系统的错误率统计 |

---

## 五、角色鉴权测试

| 测试场景 | admin | teacher1 | student1 | 未登录 |
|----------|-------|----------|----------|--------|
| 登录 | ✅ | ✅ | ✅ | — |
| 查看题目列表 | ✅ | ✅ | ✅ | ❌ 401 |
| 运行判题 | ✅ | ✅ | ✅ | ❌ 401 |
| 提交答案 | ✅ | ✅ | ✅ | ❌ 401 |
| 查看考试列表 | ✅ | ✅ (创建的) | ✅ (分配的) | ❌ 401 |
| 创建考试 | ✅ | ✅ | ❌ (前端无入口) | ❌ 401 |
| 查看班级 | ✅ | ✅ | ✅ | ❌ 401 |
| 教师仪表盘 API | ✅ | ✅ | ✅ 已拒绝 | ❌ 401 |
| 学生仪表盘 API | ✅ | ✅ | ✅ | ❌ 401 |
| 前端教师路由 | ✅ | ✅ | ⚠️ 被前端守卫拦截 | 跳转登录页 |

> 本轮复核已确认核心教师 API 增加了后端 `@PreAuthorize` 保护。

---

## 六、判题系统测试

| 测试场景 | 输入 | 结果 |
|----------|------|------|
| 正确 SELECT (但结果不匹配) | `SELECT * FROM employee` | ⚖️ WA (Wrong Answer)，得分 0 |
| 非 SELECT 语句 | `DROP TABLE employee` | 🚫 FORBIDDEN: 只允许查询类SQL |
| 空 SQL | `""` | ❌ 40000: SQL不能为空 |
| 多语句 | `SELECT 1; SELECT 2` | ❌ 40000: SQL语法错误 |
| 不存在的题目 ID | questionId=9999 | ❌ 40400: 题目不存在 |
| 关联考试提交 | questionId=1, examId=3 | ✅ 提交成功，自动触发考试计分 |

判题流程：用户 SQL → JSqlParser 语法校验 → 只允许 SELECT → 创建临时 schema → 执行测试用例的 input_sql 建表 → 执行用户 SQL → 读取结果集 → 与 expected_output JSON 比对 → 清理临时 schema → 返回结果

---

## 七、数据库状态

- **数据库**: MySQL 9.6 / `sql_exam`
- **Flyway 迁移**: 4 个脚本全部执行成功 (V1~V4)
- **用户数**: 3 个默认账号 (admin, teacher1, student1)
- **题目数**: 11 道 (1 道示例题 + 10 道 LeetCode 175-185)
- **班级数**: 1 个 ("数据库课程一班", 邀请码 `DB2026A`)
- **考试数**: 3 个 (含测试创建的)

---

## 八、总结评价

**整体完成度**: 约 **65%** — 核心判题流程完整可用，但教师端功能大量未实现，属于 MVP 阶段的半成品。

**可用场景**: 
- ✅ 学生可登录、浏览题目、在线编写 SQL 并判题、参加考试
- ✅ 教师可创建考试、添加题目和考生、查看分数
- ❌ 教师无法管理题库、无法查看详细成绩分析
- ❌ 管理员无管理界面

**优先改进建议**:

| 优先级 | 改进项 |
|--------|--------|
| **P0** | ✅ 已完成：修复异常信息泄漏（`GlobalExceptionHandler` 脱敏）+ 添加核心 API 层 `@PreAuthorize` 角色鉴权 |
| **P1** | 部分完成：✅ 补全班级加入；待补全 `ExamWizard.save()`、实现 TeacherScores 页面、添加题目 CRUD |
| **P2** | 考试作答多题切换导航、ExamWizard 剩余步骤（选题/配分）、仪表盘动态数据替换硬编码值 |
| **P3** | 服务端分页、死链接修复、密码修改功能、清理冗余依赖、统一 HTTP 状态码 |
