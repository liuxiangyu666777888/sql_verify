# 🧪 SQL Judge Exam — 第二轮验证测试报告

**测试日期**: 2026-06-13  
**测试依据**: 对照 [测试.md](测试.md) 中标记的 20 项改进逐条验证  
**测试范围**: 后端 API (20+ 端点) + 前端 14 个页面 + 3 种角色鉴权  
**服务状态**: ✅ 后端 8080 / ✅ 前端 5173 均正常运行

---

## 一、20 项改进逐条验证结果

### 🔴 原安全/严重问题 (2 项 → 全部通过)

| # | 原问题 | 验证结果 |
|---|--------|----------|
| 1 | SQL 异常信息泄漏 | ✅ **已修复**。添加不存在题目 ID 到考试返回 `{"code":50000,"message":"服务端错误，请联系管理员"}` + HTTP 500，不再暴露 SQL/表/约束名。后端 `GlobalExceptionHandler` 改为 `log.error()` 记录 + 客户端脱敏返回 |
| 2 | API 层缺少角色鉴权 | ✅ **已修复**。学生访问 `/api/teacher/dashboard` → HTTP 403；学生 `POST /api/exams` → HTTP 403；教师访问 `/api/admin/users` → HTTP 403。所有核心教师/管理员端点已添加 `@PreAuthorize` |

### 🟡 原功能桩 (7 项 → 全部通过)

| # | 原问题 | 验证结果 |
|---|--------|----------|
| 3 | ClassService.join() 空桩 | ✅ **已修复**。`join()` 现已实现：校验邀请码、查找班级、插入 student_class 记录。教师调用返回 403 |
| 4 | ExamWizard.save() 空桩 | ✅ **已修复**。`saveExam()` 调用 `POST /api/exams`，含完整校验、loading 状态、成功/错误提示。创建考试返回 `examId` + HTTP 200 |
| 5 | TeacherScores 空白页 | ✅ **已修复**。页面加载考试列表 → 下拉选择考试 → 调用 `/api/exams/{id}/scores` → 展示排名/学生/最终分/状态，计算平均分和最高分 |
| 6 | QuestionDetail 不返回 answerSql/testCases | ✅ **已修复**。DTO 增加字段；教师/管理员角色返回答案和测试用例；学生角色返回 `null`（key 存在但值为 null，无数据泄漏） |
| 7 | TeacherQuestions 无 CRUD | ✅ **已修复**。支持新增、编辑（含查看答案/测试用例）、软删除。`POST/PUT /api/questions` 需 `@PreAuthorize("hasAnyRole('TEACHER','ADMIN')")` |
| 8 | ExamTake 仅显示第一题 | ✅ **已修复**。题目列表完整加载为数组，支持题号按钮导航、上/下一题切换。每题独立保存草稿 SQL 和判题结果 |
| 9 | Exam 无编辑/删除 | ✅ **已修复**。新增 `PUT /api/exams/{id}` 编辑考试；`DELETE /api/exams/{id}` 将状态设为 `ARCHIVED` |

### 🟡 原设计/体验问题 (8 项 → 全部通过)

| # | 原问题 | 验证结果 |
|---|--------|----------|
| 10 | 仪表盘数据硬编码 | ✅ **已修复**。学生 `streakDays` 按 AC 提交日期计算（当前为 1），教师 `activeClasses` 从数据库查询（当前为 1），`pendingReviews` 动态查询 |
| 11 | 关键词搜索仅匹配标题 | ✅ **已修复**。`QuestionMapper` SQL 改为同时匹配 `title` 和 `description`。验证：搜索 "Employee" 命中 5 道题（含描述中含 Employee 的题目） |
| 12 | 题目列表无分页 | ✅ **已修复**。`/api/questions?page=&size=` 返回 `{items, total, page, size}`。前端 QuestionList 和 TeacherQuestions 均使用服务端分页 |
| 13 | 死链接 | ✅ **已修复**。AppLayout 中文档链接指向 `/problems`，帮助链接按角色指向不同页面，通知按钮显示 tooltip，设置按钮打开修改密码弹窗 |
| 14 | 无密码修改 | ✅ **已修复**。新增 `POST /api/auth/change-password` 端点，前端设置面板支持旧密码/新密码/确认密码三步验证。验证：旧密码错误拒绝、新密码过短拒绝、正常流程通过后恢复原密码 |
| 15 | 无管理员用户管理 | ✅ **已修复**。新增 `GET/POST/PUT /api/admin/users`，前端 AdminUsers.vue 支持用户列表、新增用户、角色调整、状态启用/禁用。非管理员角色访问返回 403 |
| 16 | ExamWizard 仅步骤 1 | ✅ **已修复**。三步完整流程：步骤 1 配置考试参数 → 步骤 2 分页搜索选题 → 步骤 3 设置每题顺序和分值 → 保存调用 `POST /api/exams/{id}/questions` |
| 17 | Submission snake_case | ✅ **已修复**。`/api/submissions/mine` 返回纯 camelCase 字段（`submissionId`, `submitTime`, `questionId` 等），前端不再需要兼容 snake_case |

### 🔵 原技术债务 (3 项 → 全部通过)

| # | 原问题 | 验证结果 |
|---|--------|----------|
| 18 | Flyway 版本兼容 | ✅ **已处理**。Flyway 已升级至 9.22.3，启动日志无错误，4 个迁移脚本全部验证通过 |
| 19 | 冗余依赖 | ✅ **已修复**。`echarts`、`monaco-editor` 及对应 Vite optimizeDeps 配置已移除 |
| 20 | HTTP 状态码不规范 | ✅ **已修复**。验证通过：400 Bad Request (参数校验) / 401 Unauthorized (未登录) / 403 Forbidden (权限不足) / 404 Not Found (数据不存在) / 500 Internal Server Error (服务端异常) |

---

## 二、新增功能验证

| 功能 | 端点/页面 | 验证结果 |
|------|-----------|----------|
| 密码修改 | `POST /api/auth/change-password` | ✅ 旧密码错误→400；新密码<6位→400；正常修改→200；修改后可用新密码登录 |
| 管理员用户列表 | `GET /api/admin/users` | ✅ 返回所有用户及角色/状态信息 |
| 管理员创建用户 | `POST /api/admin/users` | ✅ 创建成功返回用户对象；教师调用→403 |
| 管理员编辑用户 | `PUT /api/admin/users/{id}` | ✅ 可调整角色和状态 |
| 题目创建 | `POST /api/questions` | ✅ 教师可创建含答案的题目；学生调用→403 |
| 题目编辑 | `PUT /api/questions/{id}` | ✅ 教师可编辑题目属性及答案 |
| 题目软删除 | `DELETE /api/questions/{id}` | ✅ 删除后学生查询→404；教师列表不显示 |
| 考试编辑 | `PUT /api/exams/{id}` | ✅ 可修改考试名称/时间/时长 |
| 考试归档 | `DELETE /api/exams/{id}` | ✅ 状态变为 ARCHIVED |
| 考试向导三步流程 | `/teacher/exams/new` | ✅ 配置→选题→配分，调用真实 API |
| 考试多题作答 | `/student/exams/:id/take` | ✅ 题号导航、上/下题、独立草稿和结果 |
| 成绩统计页面 | `/teacher/scores` | ✅ 考试选择、成绩表、平均分/最高分 |
| 管理员用户管理页 | `/admin/users` | ✅ 列表+新增+编辑表单 |
| 设置面板 | AppLayout 设置按钮 | ✅ 弹窗修改密码，含确认密码校验 |

---

## 三、API 端点完整清单 (20+)

| 端点 | 方法 | HTTP | 鉴权 |
|------|------|------|------|
| `/api/auth/login` | POST | 200 | 公开 |
| `/api/auth/me` | GET | 200 | 登录 |
| `/api/auth/register` | POST | 403 | 公开(已锁定) |
| `/api/auth/change-password` | POST | 200 | 登录 |
| `/api/questions` | GET | 200 | 登录 |
| `/api/questions` | POST | 200 | TEACHER/ADMIN |
| `/api/questions/{id}` | GET | 200 | 登录 |
| `/api/questions/{id}` | PUT | 200 | TEACHER/ADMIN |
| `/api/questions/{id}` | DELETE | 200 | TEACHER/ADMIN |
| `/api/judge/run` | POST | 200 | 登录 |
| `/api/submissions` | POST | 200 | 登录 |
| `/api/submissions/mine` | GET | 200 | 登录 |
| `/api/exams` | GET | 200 | 登录 |
| `/api/exams` | POST | 200 | TEACHER/ADMIN |
| `/api/exams/{id}` | GET | 200 | 登录 |
| `/api/exams/{id}` | PUT | 200 | TEACHER/ADMIN |
| `/api/exams/{id}` | DELETE | 200 | TEACHER/ADMIN |
| `/api/exams/{id}/questions` | GET/POST | 200 | 登录/教师 |
| `/api/exams/{id}/students` | POST | 200 | TEACHER/ADMIN |
| `/api/exams/{id}/publish` | POST | 200 | TEACHER/ADMIN |
| `/api/exams/{id}/scores` | GET | 200 | TEACHER/ADMIN |
| `/api/classes` | GET | 200 | 登录 |
| `/api/classes/join` | POST | 200 | STUDENT |
| `/api/student/dashboard` | GET | 200 | 登录 |
| `/api/teacher/dashboard` | GET | 200 | TEACHER/ADMIN |
| `/api/admin/users` | GET | 200 | ADMIN |
| `/api/admin/users` | POST | 200 | ADMIN |
| `/api/admin/users/{id}` | PUT | 200 | ADMIN |

---

## 四、角色鉴权矩阵（复核）

| 测试场景 | admin | teacher1 | student1 | 未登录 |
|----------|-------|----------|----------|--------|
| 登录 | ✅ | ✅ | ✅ | — |
| 查看题目列表 | ✅ | ✅ | ✅ | HTTP 401 |
| 创建题目 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 编辑/删除题目 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 运行判题 | ✅ | ✅ | ✅ | HTTP 401 |
| 查看考试列表 | ✅ | ✅ | ✅ | HTTP 401 |
| 创建考试 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 编辑/归档考试 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 查看成绩 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 加入班级 | HTTP 403 | HTTP 403 | ✅ | HTTP 401 |
| 教师仪表盘 | ✅ | ✅ | HTTP 403 | HTTP 401 |
| 学生仪表盘 | ✅ | ✅ | ✅ | HTTP 401 |
| 管理员用户管理 | ✅ | HTTP 403 | HTTP 403 | HTTP 401 |
| 前端教师路由 | ✅ | ✅ | 重定向学生 | 重定向登录 |
| 前端管理员路由 | ✅ | 重定向教师 | 重定向学生 | 重定向登录 |

---

## 五、判题系统测试（复核）

| 测试场景 | 输入 | 结果 |
|----------|------|------|
| 正确 SELECT 但结果不匹配 | `SELECT * FROM employee` | WA, 得分 0 |
| 非 SELECT 语句 | `DROP TABLE employee` | HTTP 403, "只允许查询类SQL" |
| 空 SQL | `""` | HTTP 400, "SQL不能为空" |
| 多语句提交 | `SELECT 1; SELECT 2` | HTTP 403, "不允许多语句提交" |
| 不存在的题目 ID | questionId=99999 | HTTP 404, "题目不存在" |
| 关联考试提交 | questionId=1, examId=3 | 提交成功，自动计分 |

---

## 六、前端页面路由清单 (14 个)

| 页面 | 路由 | HTTP | 状态 |
|------|------|------|------|
| 登录页 | `/login` | 200 | ✅ |
| 题库列表(学生) | `/problems` | 200 | ✅ 服务端分页 |
| 题目详情+编辑器 | `/problems/:id` | 200 | ✅ |
| 学生仪表盘 | `/student/dashboard` | 200 | ✅ 动态指标 |
| 学生考试列表 | `/student/exams` | 200 | ✅ |
| 考试作答 | `/student/exams/:id/take` | 200 | ✅ 多题导航 |
| 学生提交记录 | `/student/submissions` | 200 | ✅ camelCase |
| 学生班级 | `/student/classes` | 200 | ✅ |
| 教师仪表盘 | `/teacher/dashboard` | 200 | ✅ 动态指标 |
| 教师题库管理 | `/teacher/questions` | 200 | ✅ 完整 CRUD |
| 教师班级管理 | `/teacher/classes` | 200 | ✅ |
| 教师成绩统计 | `/teacher/scores` | 200 | ✅ 成绩表+统计 |
| 考试配置向导 | `/teacher/exams/new` | 200 | ✅ 三步流程 |
| 管理员用户管理 | `/admin/users` | 200 | ✅ 新增/编辑/角色 |

---

## 七、现存轻微问题

| # | 类型 | 描述 |
|---|------|------|
| 1 | API 设计 | 学生题目详情 JSON 仍包含 `"answerSql":null, "testCases":null` 字段（值均为 null，无数据泄漏）。建议使用 `@JsonInclude(Include.NON_NULL)` 彻底隐藏空字段 |
| 2 | API 设计 | 题目搜索不带分页参数时返回纯数组，带分页时返回 `{items, total, page, size}` 对象。两种响应格式不一致，建议统一 |
| 3 | 前端 | StudentClasses.vue 仅展示已加入班级列表，缺少"通过邀请码加入"的输入框和交互入口（API 已支持） |
| 4 | 前端 | AppLayout 顶部搜索框仍无实际搜索功能（仅装饰性 placeholder） |

---

## 八、数据库状态

- **数据库**: MySQL 9.6 / `sql_exam`
- **Flyway 迁移**: 4 个脚本，当前版本 V4
- **用户数**: 5 (admin, teacher1, student1, l, verify_user)
- **题目数**: 13 (11 道原始 LeetCode + 2 道本次测试创建)
- **班级数**: 1 ("数据库课程一班", 邀请码 `DB2026A`)
- **考试数**: 7 (含原始种子数据 + 多轮测试创建)

---

## 九、总结评价

**整体完成度**: 约 **93%** — 相比首轮测试的 65% 有显著提升。

**首轮 20 项问题**: ✅ **全部修复验证通过**，无遗留阻塞性缺陷。

**本轮新增功能**: 密码修改、管理员用户管理、题目完整 CRUD、考试编辑/归档、考试向导三步流程、考试多题作答、成绩统计页面、设置面板 — 全部可用。

**余下轻微问题**: 4 项均为体验/设计层面，不影响核心教学流程。

**可用场景确认**:
- ✅ 学生：登录 → 浏览题库 → 在线判题 → 参加考试（支持多题切换和独立草稿）→ 查看提交记录
- ✅ 教师：登录 → 管理题库（增删改）→ 创建考试（三步向导：配置/选题/配分）→ 发布考试 → 查看成绩统计
- ✅ 管理员：登录 → 用户管理（增/改角色/改状态）→ 题目管理 → 考试管理
