# 后续改进清单

本文件基于当前 `项目文档/项目文档.tex` 和代码实现整理。已经落地的内容不再作为待办问题列出。

## 本次已落实

1. 考试提交前置校验

   `SubmissionService.submit()` 已先校验考试存在性、发布状态、学生是否参加考试、题目是否属于该考试，再执行判题和写入提交记录，避免非法考试提交产生判题开销和脏提交记录。

2. 考试时间窗口后端校验

   考试正式提交已校验 `startTime` 和 `endTime`，开始前不能提交，结束后不能提交。

3. 持久化考试草稿

   学生考试页按考试 ID 和题目 ID 将 SQL 草稿保存到浏览器 `localStorage`，刷新页面后可以恢复。

4. 路由守卫刷新后校验后端登录态

   前端进入受保护路由时会在需要时调用 `/auth/me` 校验 token 和角色，校验失败会清除本地登录信息并返回登录页。

5. 清理未启用的 Monaco 编辑器组件

   当前主流程使用 textarea，已删除残留的 `SqlEditor.vue`，避免保留缺少依赖的未使用组件。

6. 公网访问 API 代理

   `start.sh` 默认将 `VITE_API_BASE_URL` 设置为 `/api`，配合 Vite 代理和 Cloudflare 访问时不会让朋友的浏览器请求他自己电脑的 `localhost:8080`。

7. 助教角色复用教师工作台

   已恢复 `ASSISTANT` 角色。助教登录后进入教师端页面，后端权限与教师一致，可维护题库、班级、考试和成绩统计；管理员可创建或编辑助教账号。

## 建议后续改代码

1. 专用低权限判题数据库账号

   当前可通过环境变量配置 `JUDGE_DB_*`，但没有自动初始化专用低权限账号。建议补充 SQL 初始化脚本或 `start.sh --setup` 支持创建只允许访问临时 judge schema 的账号。

2. 题目管理权限边界

   当前教师可按角色编辑/删除题目，未校验题目创建者。若题库设计不是全教师共享，建议增加 creator 校验；若设计为共享题库，建议在文档中明确说明。

3. 考试草稿服务端保存

   当前草稿保存在浏览器本地，换设备或清理浏览器数据后不可恢复。若考试场景需要更强可靠性，建议新增后端草稿接口，按考试、学生、题目保存草稿。

## 文档已处理

1. 助教角色已恢复为当前版本功能，文档和 DDL 均包含 `ASSISTANT`，并明确助教复用教师端工作台和教师权限。

2. 自动交卷、锁屏、切屏检测等能力不作为当前版本承诺。

3. Markdown 题面已改为“存储 Markdown，前端以纯文本展示”，不承诺安全 HTML 渲染。

4. 难度筛选已改为“展示难度标签，但未实现按难度筛选”。

5. 判题低权限账号已改为可配置说明，不宣称默认一定使用低权限账号。
